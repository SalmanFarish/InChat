package com.example.inchat.ui.theme

import androidx.compose.ui.graphics.Color

/*
 * ============================================================
 * INCHAT MONOCHROME PALETTE
 * ============================================================
 *
 * Strict black / white / gray only.
 *
 * Designed for:
 * - AMOLED screens
 * - X-inspired visual language
 * - Minimal UI
 * - Strong text hierarchy
 * - Subtle surfaces and dividers
 */

/*
 * ============================================================
 * CORE COLORS
 * ============================================================
 */

val Black =
    Color(0xFF000000)

val White =
    Color(0xFFFFFFFF)

/*
 * Primary text / icons.
 */
val TextPrimary =
    Color(0xFFFFFFFF)

/*
 * Secondary text.
 */
val TextSecondary =
    Color(0xFFE7E7E7)

/*
 * Muted metadata / timestamps.
 */
val TextMuted =
    Color(0xFF71767B)

/*
 * Subtle surface.
 */
val SurfaceSubtle =
    Color(0xFF16181C)

/*
 * Dividers / outlines.
 */
val Divider =
    Color(0xFF2F3336)

/*
 * Slightly stronger surface.
 */
val SurfaceElevated =
    Color(0xFF202327)
